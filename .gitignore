# Node / Next.js
node_modules
.next
out

# OS cruft
.DS_Store

# Local environment secrets (root-level or nested)
env.keys
*.env
*.env.*
!.env.example

# Local private/contact data exports
private/
*.private.*

# Mailing-list / bounce / contact CSV-TXT exports at the repo root.
# These contain real subscriber PII — keep them in private/ (already
# ignored above), never commit them. See import_contacts.py / import_list.mjs.
/*.csv
/*.txt

# Build artifacts
supabase/.schema-hash
.vercel/
patch_autopush.py

# Private Mailchimp LifeX archive (campaign history, analytics, and contacts)
/mailchimp lifex gitignore me/

# Legacy directories deleted in git history
webapp
conf

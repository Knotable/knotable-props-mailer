# Node / Next.js
node_modules
.next
out

# OS cruft
.DS_Store

# Local environment secrets (root-level or nested)
env.keys
*.env
*.env.*
!.env.example

# Build artifacts
supabase/.schema-hash

# Legacy directories deleted in git history
webapp
conf
